// Firebase tests removed — Firebase has been completely removed from the project.
// Auth is now handled via JWT tokens from the GraphQL backend.
describe('Firebase (removed)', () => {
  it('should be removed', () => {
    expect(true).toBe(true);
  });
});
