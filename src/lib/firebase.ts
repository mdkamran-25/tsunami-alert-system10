// Firebase has been removed. This stub exists so any stale imports don't crash at build time.
// All auth is handled via JWT tokens stored in localStorage.

export const auth = null;
export const db = null;
export const storage = null;
export const analytics = null;
export const hasFirebaseConfig = false;
export default null;
